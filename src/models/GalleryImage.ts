import mongoose, { Schema } from 'mongoose';\n\nconst GalleryImageSchema = new Schema(\n  {\n    url: {\n      type: String,\n      required: true,\n    },\n    thumbnail: String,\n    title: String,\n    description: String,\n    category: String,\n    album: String,\n    type: {\n      type: String,\n      enum: ['image', 'video', 'drone_footage', '360_image'],\n      default: 'image',\n    },\n    hasWatermark: {\n      type: Boolean,\n      default: false,\n    },\n    displayOrder: Number,\n    compressed: {\n      type: Boolean,\n      default: false,\n    },\n  },\n  { timestamps: true }\n);\n\nexport const GalleryImage = mongoose.models.GalleryImage || mongoose.model('GalleryImage', GalleryImageSchema);\n